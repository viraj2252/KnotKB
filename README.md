# Knowledge Base

Personal knowledge base shared by Claude Code and the Hermes agent. Markdown is the
source of truth; pgvector is a rebuildable index. See `docs/reference/` for setup,
registration, and durability, and `docs/superpowers/` for the spec and plan.
